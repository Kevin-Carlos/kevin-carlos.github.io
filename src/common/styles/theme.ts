import { colors } from "./colors";

export const theme = {
  colors,
  fonts: ["Roboto Condensed", "sans-serif", "Roboto"],
  fontSizes: {
    small: "1.2rem",
    large: "2rem",
    xl: "3rem",
  }
};
