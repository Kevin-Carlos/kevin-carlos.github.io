import React from "react";
import styled from "styled-components";
import { Layout } from "common/layout";
import { ProjectDashboard } from "./_dashboard";

export default () => (
  <Layout>
    <ProjectDashboard />
  </Layout>
)
